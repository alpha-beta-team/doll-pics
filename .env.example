# Backend API base URL (includes /api prefix)
VITE_API_URL=http://localhost:3001/api

# Public site origin (no trailing slash). Used at build time for sitemap.xml,
# robots.txt, prerender canonicals/OG URLs, and runtime SEO absolute URLs.
# Set this in Netlify/Vercel production env as well.
VITE_SITE_URL=https://dollpictures.in
